[phases.setup]
nixPkgs = [
    "chromium",
    "nss",
    "freetype",
    "freetype.dev",
    "fontconfig",
    "stdenv.cc.cc.lib"
]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "PUPPETEER_EXECUTABLE_PATH=$(which chromium) npm start"
